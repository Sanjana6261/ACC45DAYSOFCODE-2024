#include <iostream>
using namespace std;

int main() {
    int T;
    cin >> T;
    while (T--) {
    int A, B, C, D, E
    If (A + B <= D && C <= E) || (A + C <= D && B <= E) || (B + C <= D && A <= E){
	
        cout<< "YES"<<endl;
    }
    Else{}
        cout<< "NO" <<endl;
    }
    return 0;
}
